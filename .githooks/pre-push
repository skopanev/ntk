#!/usr/bin/env bash
# Enable once per clone:  git config core.hooksPath .githooks
# Skip one push on purpose:  NTK_NAME_GUARD=off git push
exec python3 "$(git rev-parse --show-toplevel)/scripts/hooks/name_guard.py" "$@"
